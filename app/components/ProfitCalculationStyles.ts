import { css } from '@emotion/css';

export const profitCalculationStyles = css({
  display: 'flex',
  flexDirection: 'column',
  hr: {
    width: '100%',
  },
  p: {
    margin: '4px',
    fontWeight: 'bold',
  },
  '.profitRow': {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});
